import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export function InvoicesTab() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Invoices</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Generate and track invoices for confirmed travel packages.</p>
      </CardContent>
    </Card>
  )
} 